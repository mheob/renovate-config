{
	"$schema": "https://docs.renovatebot.com/renovate-schema.json",
	"description": "`bump` for dependencies, devDependencies, optionalDependencies and resolutions, `widen` for engines and peerDependencies, `in-range-only` for overrides. In `default.json` this comes after `base`, so it overrides the `:pinDevDependencies` that `config:best-practices` brings: devDependencies are bumped, not pinned.",
	"packageRules": [
		{
			"matchDepTypes": ["devDependencies", "dependencies", "optionalDependencies", "resolutions"],
			"matchManagers": ["npm"],
			"rangeStrategy": "bump"
		},
		{
			"matchDepTypes": ["engines", "peerDependencies"],
			"matchManagers": ["npm"],
			"rangeStrategy": "widen"
		},
		{
			"matchDepTypes": ["overrides"],
			"matchManagers": ["npm"],
			"rangeStrategy": "in-range-only"
		}
	]
}
